class Game {
  constructor(gameId, host) {
    this.gameId = gameId;
    this.host = host;
  }

  addHider(player) {
  }

  addSeeker(player) {
  }

  gameLoop() {
  }

  start() {
  }

  stop() {
  }

  addPlayer(player) {
  }

  assignRoles() {
  }
}

module.exports = { Game };
